// arrow function
const perpangkatan = (x) => {
  return x * x;
};
perpangkatan(5);
//output = 25

//return implisit(jika hanya ada satu baris syntax jika lebih tidak bisa )
const random = () => Math.floor(Math.random() * 1000);
console.log(random());
